/**@type {import('next').NextConfig}*/
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "courteous-cricket-453.convex.cloud",
      },
    ],
  },
};

export default nextConfig;
